# الدخول إلى لوحة إدارة منتجع رُومة

## رابط الدخول

- محلياً: `http://127.0.0.1:5000/admin/login`
- بعد النشر: أضف `/admin/login` إلى رابط الخادم الخلفي، مثال: `https://example.com/admin/login`

## اسم المستخدم وكلمة المرور

توجد في ملف `backend/.env`. أنشئ هذا الملف بنسخ `backend/.env.example` ثم غيّر:

- `ADMIN_USERNAME`: اسم مستخدم الإدارة.
- `ADMIN_PASSWORD`: كلمة مرور قوية وجديدة.
- `SECRET_KEY`: قيمة سرية طويلة وعشوائية.

لا ترفع ملف `.env` إلى GitHub ولا ترسل كلمة المرور لأي شخص.

## إدارة الحجوزات

من الشريط العلوي اختر **الحجوزات**. كل طلب يرسله الزائر من صفحة الحجز يُحفظ تلقائياً هنا بحالة **جديد**. يمكنك فتح الطلب، رؤية بيانات الزائر، وتغيير الحالة إلى قيد المراجعة أو مؤكد أو مرفوض أو مكتمل.

## إدارة الصور

اختر **معرض الصور**:

1. لرفع صورة جديدة اختر الصورة، اكتب وصفاً اختيارياً، ثم اضغط **رفع الصورة**.
2. لتعديل صورة موجودة غيّر الوصف أو اختر صورة بديلة، ثم اضغط **حفظ التعديل**.
3. للحذف اضغط **حذف** أسفل الصورة.

الصيغ المدعومة: PNG وJPG وJPEG وWebP، وبحجم أقصى 6 ميجابايت.

## تعديل الموقع والخريطة

اختر **المعلومات والموقع**:

1. عدّل **العنوان الظاهر للزوار**.
2. في **موقع الخريطة** اكتب اسم المكان بدقة أو الصق الإحداثيات، مثال: `33.3152,44.3661`.
3. اضغط **حفظ التغييرات**.

ستتحدث الخريطة وزر الاتجاهات في صفحة التواصل تلقائياً.

## ملاحظة عن خط ثمانية

تم ربط خط ثمانية بالموقع. راجع ترخيص الخط الرسمي قبل استخدام الموقع تجارياً أو نشر ملفات الخط بنفسك.
