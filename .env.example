# Root reference copy — the file actually read by the backend is
# backend/.env (copy backend/.env.example there and fill in real values).

SECRET_KEY=change-this-secret-key
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password
FRONTEND_ORIGIN=*
FLASK_ENV=development
PORT=5000
